# BANCO DE DADOS MYSQL
MYSQL_ROOT_PASSWORD=your_root_password
MYSQL_DATABASE=media4all_printers
MYSQL_USER=your_database_user
MYSQL_PASSWORD=your_database_password

# Configurações de DataSource do Spring Boot
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/media4all_printers?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
SPRING_DATASOURCE_USERNAME=your_database_user
SPRING_DATASOURCE_PASSWORD=your_database_password

# URL da API externa para sincronização de impressoras
# Esta URL é usada pelo backend para buscar dados de impressoras externas
EXTERNAL_API_PRINTERS_URL=https://your_external_api_url

# ============================================
# NOTA IMPORTANTE:
# ============================================
# A variável NEXT_PUBLIC_API_BASE_URL deve ser configurada
# no arquivo frontend/.env, não aqui na raiz.
# 
# Para configurar o frontend:
# 1. cp frontend/.env.example frontend/.env
# 2. Edite frontend/.env conforme necessário
